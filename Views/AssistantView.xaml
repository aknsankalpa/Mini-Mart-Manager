<UserControl x:Class="RetailFlow.Views.AssistantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="20,16,20,12">
                <TextBlock Text="🤖 MiniMart Assistant" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Text="Ask questions or tell me where you want to go."
                           FontSize="12" Foreground="#6B7280" Margin="0,2,0,0"/>
            </StackPanel>
            <Border Grid.Row="0" VerticalAlignment="Bottom" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1"/>

            <!-- Conversation -->
            <ScrollViewer x:Name="ConversationScrollViewer" Grid.Row="1"
                          VerticalScrollBarVisibility="Auto" Padding="20,12">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,6">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <TextBlock FontSize="11" Foreground="#9CA3AF" Margin="4,0,4,2">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="MiniMart Assistant"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                    <Setter Property="Text" Value="You"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <Border Padding="12,9" CornerRadius="10" MaxWidth="440" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#F3F4F6"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                    <Setter Property="Background" Value="#2563EB"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <StackPanel>
                                        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" FontSize="13">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#111827"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <Button Content="{Binding ActionButtonText}"
                                                Command="{Binding ActionCommand}"
                                                HorizontalAlignment="Left" Margin="0,10,0,0">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ActionButtonText}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Input area -->
            <StackPanel Grid.Row="2" Margin="20,0,20,20">
                <WrapPanel Margin="0,0,0,10">
                    <Button Content="💰 Today's Sales" Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,8,0" Command="{Binding QuickActionCommand}"
                            CommandParameter="how much did we sell today"/>
                    <Button Content="⚠️ Low Stock" Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,8,0" Command="{Binding QuickActionCommand}"
                            CommandParameter="show low stock products"/>
                    <Button Content="🔍 Find Product" Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,8,0" Command="{Binding QuickActionCommand}"
                            CommandParameter="find "/>
                    <Button Content="🧾 Transactions" Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding QuickActionCommand}"
                            CommandParameter="show transactions"/>
                </WrapPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="AssistantInputTextBox" Grid.Column="0"
                             Style="{StaticResource FormTextBoxStyle}"
                             Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,10,0"
                             KeyDown="AssistantInputTextBox_KeyDown"/>

                    <Button x:Name="SendButton" Grid.Column="1" Content="Send"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding SendCommand}"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
